export enum BillingCycleTypes {
    Monthly = 'monthly',
    Yearly = 'yearly'
}

export enum RecurrenceInterval {
  Daily = "Daily",
  Weekly = "Weekly",
  Monthly = "Monthly",
  Yearly = "Yearly"
}

export enum TransactionType {
  Revenue = "Revenue",
  Expense = "Expense"
}
